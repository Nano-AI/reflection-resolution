package com.example.app;

/** Target of the OWN_CODE case in Sample.java. Exists so that same-package
 *  name resolution has something real to resolve against. */
public class Order {
    private void recalculate() { }
}
